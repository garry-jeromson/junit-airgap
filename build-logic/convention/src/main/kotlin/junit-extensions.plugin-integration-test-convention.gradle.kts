/**
 * Convention plugin for plugin integration test projects.
 *
 * Note: Plugin integration test projects are now in an included build
 * that consumes the plugin from Maven Local. The root build handles
 * publishing before running tests.
 */
